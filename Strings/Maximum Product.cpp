#include<bits/stdc++.h>

using namespace std;

int main(){
    int arr[] = {6, -3, -10, 0, 2};
    int n=sizeof(arr)/sizeof(arr[0]);

    int start=0;

    int it=0;
    int product=1;
    while(it<n){
        while(arr[it]!=0 && it<n){

        }
        if(arr[it]==0){start=i+1;}
        it++;
    }

    return 0;
}
